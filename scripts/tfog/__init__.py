"""tfog — the local, single-operator implementation of the terraform-on-github flow.

Two scripts, `tfog-plan` and `tfog-apply`, do on one machine what docs/DESIGN.md splits across
two Cloud Run services. See docs/LOCAL.md for what is preserved and what is deliberately gone.
"""
